<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TitleTempMapper">
	
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into TB_TITLETEMP(
			TITLE_CATEGORY_ID,	
			TITLE_NAME,	
			CONTENT,	
			CREATE_TIME 
		) values (
			#{TITLE_CATEGORY_ID},	
			#{TITLE_NAME},	
			#{CONTENT},	
			#{CREATE_TIME} 
		)
	</insert>
	
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from TB_TITLETEMP
		where 
			TITLE_ID = #{TITLE_ID}
	</delete>
	
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update  TB_TITLETEMP
			set 
			<if test="TITLE_CATEGORY_ID!=null and TITLE_CATEGORY_ID!=''">
				TITLE_CATEGORY_ID = #{TITLE_CATEGORY_ID},
			</if>
			<if test="TITLE_NAME!=null and TITLE_NAME!=''">
				TITLE_NAME = #{TITLE_NAME},
			</if>
			<if test="CONTENT!=null and CONTENT!=''">
				CONTENT = #{CONTENT},
			</if>
			<if test="USE_TIMES!=null and USE_TIMES!=''">
				USE_TIMES = #{USE_TIMES},
			</if>
			TITLE_ID = TITLE_ID
			where 
				TITLE_ID = #{TITLE_ID}
	</update>
	
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
			TITLE_CATEGORY_ID,	
			TITLE_NAME,	
			CONTENT,	
			USE_TIMES,	
			CREATE_TIME,	
			TITLE_ID
		from 
			TB_TITLETEMP
		where 
			TITLE_ID = #{TITLE_ID}
	</select>
	
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
				a.TITLE_CATEGORY_ID,	
				a.TITLE_NAME,	
				a.CONTENT,	
				a.USE_TIMES,	
				a.CREATE_TIME,	
				a.TITLE_ID,
				b.CATEGORY_NAME
		from 
				TB_TITLETEMP a,TB_TITLE_CATEGORY b
		where
				a.TITLE_CATEGORY_ID=b.TITLE_CATEGORY_ID
				<if test="pd.TITLE_CATEGORY_ID!=null and pd.TITLE_CATEGORY_ID!=''">
				and a.TITLE_CATEGORY_ID=#{pd.TITLE_CATEGORY_ID}
				</if> 
				<if test="pd.CATEGORY_NAME!=null and pd.CATEGORY_NAME!=''">
				and b.CATEGORY_NAME=#{pd.CATEGORY_NAME}
				</if>
				<if test="pd.CONTENT!=null and pd.CONTENT!=''">
				and a.CONTENT  LIKE '%'||#{pd.CONTENT}||'%'
				</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
				a.TITLE_CATEGORY_ID,	
				a.TITLE_NAME,	
				a.CONTENT,	
				a.USE_TIMES,	
				a.CREATE_TIME,	
				a.TITLE_ID,
				b.CATEGORY_NAME
		from 
				TB_TITLETEMP a,TB_TITLE_CATEGORY b
		where
				a.TITLE_CATEGORY_ID=b.TITLE_CATEGORY_ID
				<if test="TITLE_CATEGORY_ID!=null and TITLE_CATEGORY_ID!=''">
				and a.TITLE_CATEGORY_ID=#{TITLE_CATEGORY_ID}
				</if> 
				<if test="CATEGORY_NAME!=null and CATEGORY_NAME!=''">
				and b.CATEGORY_NAME=#{CATEGORY_NAME}
				</if>
				<if test="CONTENT!=null and CONTENT!=''">
				and a.CONTENT  LIKE '%'||#{CONTENT}||'%'
				</if>
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from TB_TITLETEMP
		where 
			TITLE_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	<!-- 清空表 -->
	<delete id="delAll" parameterType="pd">
		 delete from TB_TITLETEMP where title_id>0
	</delete>
</mapper>