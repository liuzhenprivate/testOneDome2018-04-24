<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PicMapper">
	
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into TB_PIC(
			PIC_CATEGORY_ID,	
			PIC_NAME,	
			PIC_URL,	
			CREATE_TIME 
		) values (
			#{PIC_CATEGORY_ID},	
			#{PIC_NAME},	
			#{PIC_URL},	
			#{CREATE_TIME} 
		)
	</insert>
	
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from TB_PIC
		where 
			PIC_ID = #{PIC_ID}
	</delete>
	
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update  TB_PIC
			set 
			<if test="PIC_CATEGORY_ID!=null and PIC_CATEGORY_ID!=''">
				PIC_CATEGORY_ID = #{PIC_CATEGORY_ID},
			</if>
			<if test="PIC_NAME!=null and PIC_NAME!=''">
				PIC_NAME = #{PIC_NAME},
			</if>
			<if test="PIC_URL!=null and PIC_URL!=''">
				PIC_URL = #{PIC_URL},
			</if>
			<if test="USE_TIMES!=null and USE_TIMES!=''">
				USE_TIMES = #{USE_TIMES},
			</if>
				PIC_ID = PIC_ID
			where 
				PIC_ID = #{PIC_ID}
	</update>
	
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
			PIC_CATEGORY_ID,	
			PIC_NAME,	
			PIC_URL,	
			USE_TIMES,	
			CREATE_TIME,	
			PIC_ID
		from 
			TB_PIC
		where 
			PIC_ID = #{PIC_ID}
	</select>
	
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
				a.PIC_CATEGORY_ID,	
				a.PIC_NAME,	
				a.PIC_URL,	
				a.USE_TIMES,	
				a.CREATE_TIME,	
				a.PIC_ID,
				b.CATEGORY_NAME
		from 
				TB_PIC a,TB_PIC_CATEGORY b
		where   a.PIC_CATEGORY_ID=b.PIC_CATEGORY_ID
			<if test="pd.CATEGORY_NAME!=null and pd.CATEGORY_NAME!=''">
				and b.CATEGORY_NAME=#{pd.CATEGORY_NAME}
			</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
				a.PIC_CATEGORY_ID,	
				a.PIC_NAME,	
				a.PIC_URL,	
				a.USE_TIMES,	
				a.CREATE_TIME,	
				a.PIC_ID,
				b.CATEGORY_NAME
		from 
				TB_PIC a,TB_PIC_CATEGORY b
		where   a.PIC_CATEGORY_ID=b.PIC_CATEGORY_ID
			<if test="CATEGORY_NAME!=null and CATEGORY_NAME!=''">
				and b.CATEGORY_NAME=#{CATEGORY_NAME}
			</if>
			<if test="PIC_CATEGORY_ID!=null and PIC_CATEGORY_ID!=''">
				and a.PIC_CATEGORY_ID=#{PIC_CATEGORY_ID}
			</if>
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from TB_PIC
		where 
			PIC_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
</mapper>