<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="LabelCategoryMapper">
	
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into TB_LABEL_CATEGORY(
			CATEGORY_NAME
		) values (
			#{CATEGORY_NAME}
		)
	</insert>
	
	
	<!-- 删除-->
	<delete id="delete" parameterType="pd">
		delete from TB_LABEL_CATEGORY
		where 
			LABEL_CATEGORY_ID = #{LABEL_CATEGORY_ID}
	</delete>
	
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update  TB_LABEL_CATEGORY
			set 
				CATEGORY_NAME = #{CATEGORY_NAME}
			where 
				LABEL_CATEGORY_ID = #{LABEL_CATEGORY_ID}
	</update>
	
	
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select 
			LABEL_CATEGORY_ID,	
			CATEGORY_NAME
		from 
			TB_LABEL_CATEGORY
		where 
			LABEL_CATEGORY_ID = #{LABEL_CATEGORY_ID}
	</select>
	
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
				a.LABEL_CATEGORY_ID,	
				a.CATEGORY_NAME
		from 
				TB_LABEL_CATEGORY a
		where 1=1
				<if test="pd.CATEGORY_NAME!='' and pd.CATEGORY_NAME!=null">
					and a.CATEGORY_NAME = #{pd.CATEGORY_NAME}
				</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
				a.LABEL_CATEGORY_ID,	
				a.CATEGORY_NAME
		from 
				TB_LABEL_CATEGORY a
	</select>
	
	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from TB_LABEL_CATEGORY
		where 
			LABEL_CATEGORY_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- 通过标签类名获取数据 -->
	<select id="findCategoryName" parameterType="pd" resultType="pd">
		select 
			LABEL_CATEGORY_ID,	
			CATEGORY_NAME
		from 
			TB_LABEL_CATEGORY
		where 1=1
			<if test="CATEGORY_NAME!='' and CATEGORY_NAME!=null">
				and CATEGORY_NAME = #{CATEGORY_NAME}
			</if>
	</select>
	<!--  删除标签时删除标签类关联表-->
	<delete id="deleteCategoryLabelid" parameterType="pd">
		delete from TB_LABEL_CATEGORY
		where 
			LABEL_ID = #{LABEL_ID}
	</delete>
</mapper>